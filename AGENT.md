# AGENT.md — 工作守则

## 删除操作铁律（2026-08-29 删库事故的教训）

**事故记录**：2026-08-29，在 dshPlugins/cc-migrate 任务中，一个为沙箱数据库写的"清空全部会话"脚本
（`DELETE FROM session`，无任何 WHERE 限定、无路径守卫）被原样复用到了生产库
`C:\Users\FishBottle\.local\share\opencode\opencode.db`，删除了 1,410 个真实会话。
靠两个未被波及的备份源救回 1,047 个（event 事件日志 275 个 + storage/ 旧版文件存储 772 个），
363 个会话最终未能恢复。

**由此定下的规则，任何删除类操作（DELETE / TRUNCATE / DROP / rm -rf / 覆盖式写入）执行前必须全部满足：**

1. **三重核对目标**：写出即将执行的确切 SQL/命令与**解析后的绝对路径**，确认它与意图中的目标一致。
   沙箱路径和生产路径绝不混用同一个脚本参数。
2. **删除脚本必须自缚手脚**：目标绝对路径硬编码进脚本并启动时断言（assert），不匹配即拒绝运行；
   禁止裸 `DELETE FROM <table>`，必须带精确 WHERE（如 `id IN (…)` 白名单）；
   执行前先 `SELECT COUNT(*)` 打印影响范围。
3. **删前先备份**：对任何不在本工作区、或不可再生的数据，删除/覆盖前先做文件级拷贝并记录哈希。
4. **一次性脚本用完即删**：带破坏性的临时脚本完成任务后立即删除，不留被无意复用的机会；
   需要保留的只留只读脚本。
5. **"沙箱里能跑"不等于"哪里都能跑"**：参数化脚本的威力来自参数，也死于参数——
   换目标 = 重新走一遍第 1 条。

## 其他

- 涉及外部工具真实数据（如 opencode.db、~/.dsh）的操作，默认视为不可再生数据。
- 对外部工具做适配前，先读它的源码/真实数据样本核对契约，不靠猜（本次 opencode 适配反复踩坑的根源）。
