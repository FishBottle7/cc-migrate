# cc-migrate DSH plugin — bundle patch: insert the plugin row (deployment-global).
#
# Registers the /cc-migrate slash commands (list-sources / preview / import).
# All commands are read-old-write-new: they never touch, rewrite or delete
# existing sessions on either side. Override options via the config object.
- insert:
    - id: cc-migrate
      name: '@cc-migrate/dsh-plugin'
      config: {}
