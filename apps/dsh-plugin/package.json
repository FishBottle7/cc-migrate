{
  "name": "@cc-migrate/dsh-plugin",
  "version": "0.1.0",
  "description": "cc-migrate DSH plugin: bring any AI coding tool's session into DSH as a resumable native session.",
  "type": "module",
  "main": "lib/index.js",
  "exports": {
    ".": "./lib/index.js",
    "./gui": "./lib/gui.js",
    "./cordis.patch.yml": "./cordis.patch.yml",
    "./package.json": "./package.json"
  },
  "files": [
    "lib",
    "cordis.patch.yml",
    "README.md"
  ],
  "engines": {
    "node": ">=22"
  },
  "dsh": {
    "bundle": {
      "patch": "./cordis.patch.yml"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "bundle": "tsdown",
    "pack": "pnpm run build:smoke && pnpm run bundle && node scripts/pack.mjs",
    "build:smoke": "pnpm run build && node test/smoke.mjs && node test/gui-smoke.mjs",
    "test": "pnpm run build:smoke"
  },
  "keywords": [
    "dsh",
    "dsh-plugin",
    "cc-migrate",
    "cc-migrate",
    "cordis",
    "cordis-plugin",
    "deepseek-harness"
  ],
  "license": "MIT",
  "dependencies": {
    "@cc-migrate/core": "workspace:*",
    "@cc-migrate/ui": "workspace:*",
    "vue": "^3.5.0"
  },
  "peerDependencies": {
    "@cc-migrate/ui": "*",
    "vue": "^3.5.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsdown": "^0.22.0",
    "typescript": "^5.6.0"
  }
}
