node_modules/
.pnpm-store/
dist/
lib/
*.tsbuildinfo
.DS_Store

# scratch / probes
docs/_probe*
.env
# audit scratch homes & probe scripts
.tmp-*/
packages/core/scratch/
# 08-30 opencode parent_id 抢救的本地残留（docs/agents/opencode.md 有引用，仅本地保留）
.db-rescue/

# generic temp files / editor droppings
*.tmp
*.log
*.bak
*.swp
*.orig
*.rej
.cache/
Thumbs.db

# oversized build artifacts (git 无法按大小忽略，按产物类型兜底)
*.min.js.map
*.bundle

# electron-builder 产物（apps/desktop-app/release）
apps/desktop-app/release/
*.tgz

# 插件包内的通用 skill 副本 —— build 期由 scripts/sync-universal-skill.mjs
# 从 packages/cli/skills/cc-migrate/SKILL.md 强制同步（单一事实源在独立 CLI）
apps/dsh-plugin/skills/universal/
